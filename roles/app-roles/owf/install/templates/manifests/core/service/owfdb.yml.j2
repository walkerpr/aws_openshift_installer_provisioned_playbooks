---

apiVersion: v1
kind: Service
metadata:
  annotations:
    version: {{ sample_version }}
  labels:
    app: owfdb
  name: owfdb
  namespace: application-sample
spec:
  ports:
    - name: 5432-tcp
      port: 5432
      protocol: TCP
      targetPort: 5432
  selector:
    app: owfdb
  sessionAffinity: None
  type: ClusterIP

