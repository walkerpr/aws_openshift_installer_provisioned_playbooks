---

apiVersion: v1
kind: Service
metadata:
  annotations:
    version: {{ sample_version }}
  labels:
    app: marketplace
  name: marketplace
  namespace: application-sample
spec:
  ports:
    - name: 8009-tcp
      port: 8009
      protocol: TCP
      targetPort: 8009
  selector:
    app: marketplace
  sessionAffinity: None
  type: ClusterIP

