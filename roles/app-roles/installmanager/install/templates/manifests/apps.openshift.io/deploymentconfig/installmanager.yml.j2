---

apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  annotations:
    version: {{ sample_version }}
  labels:
    app: installmanager
  name: installmanager
  namespace: application-sample
spec:
  replicas: 1
  selector:
    app: installmanager
    deploymentconfig: installmanager
  strategy:
    type: Rolling
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 600
  template:
    metadata:
      labels:
        app: installmanager
        deploymentconfig: installmanager
    spec:
      containers:
        - env:
            - name: new_truststore_password
              valueFrom:
                secretKeyRef:
                  key: password
                  name: newtruststorepassword
            - name: sp_key_password
              valueFrom:
                secretKeyRef:
                  key: password
                  name: spkeypassword
            - name: artpass
              valueFrom:
                secretKeyRef:
                  key: password
                  name: artpass
            - name: PARENT_ARTPASS
              valueFrom:
                secretKeyRef:
                  key: password
                  name: parent-artpass
            - name: DBPASS
              valueFrom:
                secretKeyRef:
                  key: password
                  name: dbpass
            - name: new_keystore_password
              valueFrom:
                secretKeyRef:
                  key: password
                  name: newkeystorepassword
            - name: key_password
              valueFrom:
                secretKeyRef:
                  key: password
                  name: keypassword
            - name: keystore_password
              valueFrom:
                secretKeyRef:
                  key: password
                  name: keystorepassword
            - name: SERVICE_REGISTRY_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: service-registry-password
          envFrom:
            - configMapRef:
                name: aaproperties
          image: >-
            {{ pull_repository }}/installmanager:{{ sample_version }}
          imagePullPolicy: Always
          livenessProbe:
            exec:
              command:
                - /opt/applicationsample/healthcheck.sh
            initialDelaySeconds: 1200
            periodSeconds: 20
            timeoutSeconds: 5
          name: installmanager
          ports:
            - containerPort: 8009
              protocol: TCP
          readinessProbe:
            exec:
              command:
                - /opt/applicationsample/healthcheck.sh
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 57
          resources: {}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          volumeMounts:
            - mountPath: /usr/share/tomcat/logs
              name: volume-data
              subPath: installmanager/tomcat8/logs
            - mountPath: /opt/applicationsample/certs/server
              name: volume-installmanagercerts
            - mountPath: /usr/share/tomcat/tmp
              name: installmanager-tomcat-tmp
            - mountPath: /usr/share/tomcat/webapps/install-manager/tmp
              name: installmanager-banner
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext:
        supplementalGroups: [ 65534 ]
      serviceAccount: installmanager
      serviceAccountName: installmanager
      terminationGracePeriodSeconds: 30
      volumes:
        - name: volume-data
          persistentVolumeClaim:
            claimName: data
        - configMap:
            defaultMode: 420
            items:
              - key: cabundle.crt.tmp
                path: cabundle.crt.tmp
              - key: idp.crt
                path: {{ idp_alias }}.crt
              - key: server_private_key
                path: {{ server_alias }}.key
              - key: server_certificate
                path: {{ server_alias }}.crt
            name: certificates
          name: volume-installmanagercerts
        - emptyDir: {}
          name: installmanager-tomcat-tmp
        - emptyDir: {}
          name: installmanager-banner
