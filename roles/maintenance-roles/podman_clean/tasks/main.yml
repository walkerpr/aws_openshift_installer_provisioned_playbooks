---
- name: Remove Podman Images
  become: true
  register: my_output
  changed_when: my_output.rc != 0
  ansible.builtin.command: podman rmi "{{ item.name }}:{{ tag }}"
  loop: "{{ podman_images_to_remove }}"
  ignore_errors: true
