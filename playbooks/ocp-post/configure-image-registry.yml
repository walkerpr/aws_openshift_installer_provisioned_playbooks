---

- hosts: localhost
  gather_facts: no
  roles:
    - ocp-post-roles/configure_image_registry
  environment:
    KUBECONFIG: "/home/{{ ansible_user }}/.kube/deployment{{ deployment_number }}-ocp.conf"