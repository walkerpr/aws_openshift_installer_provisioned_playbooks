---

- name: Play.
  hosts: localhost
  gather_facts: no
  roles:
    - ocp-post-roles/add_graceful_shutdown
  environment:
    KUBECONFIG: "/home/{{ ansible_user }}/.kube/deployment{{ deployment_number }}-ocp.conf"