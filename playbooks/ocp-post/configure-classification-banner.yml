---
- name: Play.
  hosts: localhost
  gather_facts: false
  roles:
    - ocp-post-roles/configure_classification_banner
  environment:
    KUBECONFIG: "/home/{{ ansible_user }}/.kube/deployment{{ deployment_number }}-ocp.conf"
