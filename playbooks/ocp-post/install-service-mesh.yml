---
- name: Play.
  hosts: localhost
  gather_facts: false
  roles:
    - ocp-post-roles/install_service_mesh
  environment:
    KUBECONFIG: "/home/{{ ansible_user }}/.kube/deployment{{ deployment_number }}-ocp.conf"
